# URL base da sua API Spring Boot de contato (sem barra no final).
# Se ficar vazio, o formulário roda em modo de demonstração (sem IA real e sem envio).
NEXT_PUBLIC_CONTACT_API_URL=http://localhost:8080
